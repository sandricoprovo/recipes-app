import React from 'react';
import { HeaderContainer } from './Header.styled';

export function Header() {
    return (
        <HeaderContainer>
            <h1>Recipes Search App</h1>
            <p>Use this app to search for some fun recipes!</p>
        </HeaderContainer>
    );
}
